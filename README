##############################################
#                                            #
# CYANOGENMOD 7 FOR SAMSUNG GALAXY S SERIES  #
#                                            #
##############################################

PREPARE YOUR BUILD SYSTEM:
    sudo add-apt-repository "deb http://archive.canonical.com/ lucid partner"
    sudo apt-get update
    sudo apt-get install git-core gnupg flex bison gperf libsdl-dev libesd0-dev libwxgtk2.6-dev squashfs-tools build-essential zip curl libncurses5-dev zlib1g-dev sun-java6-jdk pngcrush g++-multilib lib32z1-dev lib32ncurses5-dev lib32readline5-dev gcc-4.3-multilib g++-4.3-multilib

CREATE DIRECTORIES:
    mkdir -p ~/bin 
    mkdir -p ~/android/system 
    cd ~/android/system 

INSTALL REPO:
    curl http://android.git.kernel.org/repo > ~/bin/repo 
    chmod a+x ~/bin/repo
    sudo reboot now

INIT REPO:
    repo init -u git://github.com/coolya/android.git
    repo sync -j40

BUILD FOR GALAXYS:
    . build/envsetup.sh 
    lunch cyanogen_galaxys-eng
    make -j`grep 'processor' /proc/cpuinfo | wc -l`

BUILD FOR CAPTIVATE:
    . build/envsetup.sh 
    lunch cyanogen_captivate-eng
    make -j`grep 'processor' /proc/cpuinfo | wc -l`

##############################################
#                                            #
# KERNEL 2.6.35 FOR SAMSUNG GALAXY S SERIES  #
#                                            #
##############################################

PREPARE:
    cd ~/android/system/kernel/samsung/2.6.35
    ./build_galaxys.sh clean

BUILD FOR GALAXYS:
    ./build_galaxys.sh
Note: Initramfs will be taken from device/samsung/galaxys/initramfs

BUILD FOR CAPTIVATE:
    ./build_captivate.sh
Note: Initramfs will be taken from device/samsung/captivate/initramfs


